## 1. Configuration Model

- [x] 1.1 Add `DidValueProviderConfig` to `SimulatorConfig.cs` with provider type, numeric type, random, sine, linear, and seed fields.
- [x] 1.2 Add supported numeric type parsing and byte-length helpers for `uint8`, `uint16`, `int16`, `uint32`, and `int32`.
- [x] 1.3 Preserve existing static DID behavior when `valueProvider` is omitted or set to `static`.

## 2. Configuration Validation

- [x] 2.1 Update DID validation so static DIDs still require `valueEncoding=hex` and a valid even-length hex `value`.
- [x] 2.2 Add validation for `random` provider required fields, `min <= max`, supported `numericType`, and numeric range fit.
- [x] 2.3 Add validation for `sine` provider required fields, positive `periodMs`, supported `numericType`, and output range fit.
- [x] 2.4 Add validation for `linear` provider required fields and supported `numericType`.
- [x] 2.5 Reject writable dynamic DIDs with a field-level validation error.

## 3. Runtime DID Value Generation

- [x] 3.1 Extend `DidRuntimeStore` entries to represent static and dynamic value sources.
- [x] 3.2 Implement read-time generated values for `random`, including seeded repeatability.
- [x] 3.3 Implement read-time generated values for `sine` based on elapsed runtime time.
- [x] 3.4 Implement read-time generated values for `linear` based on elapsed runtime time.
- [x] 3.5 Encode generated numeric samples to big-endian bytes according to `numericType`.
- [x] 3.6 Ensure `TryRead`, `List`, `GET /api/dids`, and UDS `0x22` consume generated values through the runtime store.
- [x] 3.7 Keep API and UDS writes limited to static writable DIDs and reject dynamic DID write attempts clearly.

## 4. Sample Configuration

- [x] 4.1 Add at least two dynamic DID examples to `sample-config/default.simulator.json`.
- [x] 4.2 Ensure the default/sample configuration validates after dynamic DID examples are added.

## 5. Tests And Verification

- [x] 5.1 Add configuration validation tests for valid and invalid static, random, sine, and linear DID provider definitions.
- [x] 5.2 Add runtime store tests for random range and seeded repeatability.
- [x] 5.3 Add runtime store tests for sine and linear generated byte lengths and value behavior.
- [x] 5.4 Add regression tests proving existing static DID reads and writable static DID updates still work.
- [x] 5.5 Add tests proving dynamic DID write attempts are rejected or invalid by configuration.
- [x] 5.6 Run `dotnet test .\DoipSimulator.sln --no-restore`.
- [x] 5.7 Run `openspec validate phase2-dynamic-did-provider --strict`.
